import { home as homePath } from "./paths"


export const components = []

export const description = 'Epps is Pinia plugin. It extends Pinia stores with advanced features such as persistence, encryption, and store extension'

export const id = 'home'

export const path = homePath

export const title = 'Home'

export default {
    components,
    description,
    id,
    path,
    title
}